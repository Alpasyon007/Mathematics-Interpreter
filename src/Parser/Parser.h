#pragma once

#include <vector>
#include "../Token/Token.h"

class Parser {
    private:
    private:
    public:
        // Constructor
        Parser();
        void ParseTokenVec(std::vector<Token> tokenVec); 
};